# REL-0003 — 0.2.0 Release Readiness Evidence

**Status:** READY FOR PUBLICATION APPROVAL / NOT PUBLISHED

## 1. Candidate Identity

- Approved package version: `0.2.0`
- Intended tag: `v0.2.0`
- Exact artifact/tag candidate SHA: `1faf14c121b7b5da7c8781e3de4e836f85838a76`
- Canonical branch at candidate selection: `master`
- Previous published release: `v0.1.0` at `73929bd15fa7637db8162aac199697582bb25e67`
- PyPI: **NOT APPROVED / NOT PUBLISHED**

This evidence document is intentionally recorded after candidate verification. The documentation commit containing this file is not the artifact/tag target; the release candidate remains the exact SHA above unless a release-affecting change invalidates it.

## 2. Candidate Scope

The 0.2.0 candidate preserves the previously published 0.1.0 foundation and adds the verified post-0.1.0 work intended for this release line, including:

- AUTO-0001 Engineering Project Bootstrap;
- AUTO-0002 Project Documentation Synchronization;
- SAFE-0002 Git/Python Execution Safety;
- cross-platform and integration hardening completed before candidate selection;
- the 0.2.0 package/MCP version alignment and release-test version generalization required by REL-0003.

No new feature was added during final release-readiness verification.

## 3. Version Consistency

The candidate records version `0.2.0` consistently across the approved release surfaces:

- `pyproject.toml` project version: `0.2.0`;
- `uv.lock` local `ai-engineering` package version: `0.2.0`;
- active MCP server version metadata: `0.2.0`;
- expected Git tag: `v0.2.0`;
- distribution verification derives and validates the artifact/installed version from `pyproject.toml` rather than retaining a historical hard-coded 0.1.0 assertion.

The `uv.lock` refresh changed only the local `ai-engineering` package version from `0.1.0` to `0.2.0`; no dependency upgrade was introduced as part of the version refresh.

## 4. Linux GitHub Actions Evidence

Candidate preparation PR #55 passed Quality run #78 before merge.

The exact candidate commit `1faf14c121b7b5da7c8781e3de4e836f85838a76` then passed post-merge Quality run #79 on GitHub Actions/Linux/Python 3.11.

The Quality workflow includes:

- locked development dependency synchronization;
- Ruff;
- mypy;
- the full pytest suite, including REL-0001 distribution verification.

Run #79 conclusion: **SUCCESS**.

## 5. Windows Candidate Evidence

Fresh Windows-local verification was executed against the exact candidate SHA after merge.

Environment:

- Windows
- Python 3.11.9
- pytest 9.1.1

Results:

- full pytest: **153 passed, 2 skipped** out of 155 collected;
- Ruff: **0 findings**;
- mypy: **0 issues in 79 source files**;
- focused release distribution test: **1 passed**;
- `git diff --check`: passed;
- final `git status --short`: clean.

The two skipped tests are the already classified privilege-dependent symlink fixtures:

- SAFE-0002 Python symlink-escape fixture;
- SAFE-0001 Workspace symlink-escape fixture.

Both skips report Windows `WinError 1314` because the local process lacks symlink-creation privilege. Equivalent link-escape coverage executes in Linux CI. No unexpected Windows skip or failure is recorded.

## 6. Distribution and Installed-Wheel Evidence

The release verification test passed for the 0.2.0 candidate and therefore verified the REL-0001 artifact contract for the current project version.

Verified candidate artifact names are:

- `ai_engineering-0.2.0-py3-none-any.whl`
- `ai_engineering-0.2.0.tar.gz`

The passing release test verifies, among other bounded checks:

- wheel and sdist construction;
- approved wheel and sdist contents/exclusions;
- distribution name `ai-engineering`;
- installed version `0.2.0`;
- Python requirement `>=3.11`;
- MCP runtime dependency metadata;
- console entry point `ai-engineering = ai_engineering.cli:main`;
- installation of the wheel into a fresh external virtual environment;
- import resolution from the isolated installed environment rather than repository `src`;
- installed CLI discovery;
- installed `project create` smoke;
- installed `project bootstrap` smoke;
- installed AUTO-0002 `project docs check/plan/apply` behavior outside the source checkout.

Network access may be required for isolated build/install dependency resolution. Offline release verification is not claimed.

## 7. Safety and Compatibility Boundary

The candidate preserves SAFE-0001 and SAFE-0002 authorization contracts.

These boundaries restrict MCP-exposed filesystem/Git/Python operations to their approved authority roots and subprocess rules. They are not an operating-system sandbox and do not claim containment of malicious code that is already authorized to execute inside the workspace.

Client interoperability claims remain bounded to the specifically recorded VS Code and Antigravity evidence. No new client compatibility claim is introduced by 0.2.0 readiness.

## 8. Publication State

At the time this readiness record is created:

- `v0.2.0` tag: **NOT CREATED**;
- GitHub Release `AI-Engineering 0.2.0`: **NOT CREATED**;
- GitHub Release assets: **NOT UPLOADED**;
- TestPyPI: **NOT APPROVED / NOT PUBLISHED**;
- PyPI: **NOT APPROVED / NOT PUBLISHED**.

Successful readiness does not itself authorize publication.

## 9. Publication Approval Gate

The candidate is **READY FOR PUBLICATION APPROVAL** for the bounded GitHub release surface because version consistency, Linux CI, Windows verification, distribution artifacts, isolated installed metadata/CLI behavior, and release notes preparation are satisfied.

Immediately before publication, explicit approval is still required for:

1. creating tag `v0.2.0` targeting exact candidate SHA `1faf14c121b7b5da7c8781e3de4e836f85838a76`;
2. creating GitHub Release `AI-Engineering 0.2.0` from that tag;
3. uploading the verified wheel and sdist assets, if assets are included in the approved publication action.

PyPI remains outside that approval and must not be published.

## 10. Candidate Invalidation Rule

The exact candidate SHA is invalidated if any source, package metadata, lockfile, workflow, artifact-content policy, runtime behavior, or release-relevant change intended to be part of 0.2.0 is made after candidate selection.

Evidence-only documentation commits may follow without changing the artifact/tag target, provided they explicitly retain the exact candidate SHA and do not claim that their own SHA is the built artifact source.

## 11. Readiness Result

**REL-0003-02 result: READY FOR PUBLICATION APPROVAL / NOT PUBLISHED.**

No tag, GitHub Release, asset upload, registry publication, credential, or publishing automation is created by this evidence task.
